# dependencies
node_modules/
.npm/

# build output
dist/
build/
*.tsbuildinfo
.next/
.next-stat/
# Generated by every `next build` with a reference to its own build directory (PH-24.14).
apps/web/next-env.d.ts
out/

# test / coverage
coverage/
.vitest/

# generated simulation artifacts and evidence blobs
artifacts/
*.ndjson
*.ndjson.gz

# environment / secrets
.env
.env.*
!.env.example
secrets/
*.key
*.pem

# editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# logs
*.log
npm-debug.log*

# agent worktrees and session scratch (Cycle Audit 7: eight audit trees appeared
# as untracked files inside the repository, one `git add -A` from being committed)
.claude/
