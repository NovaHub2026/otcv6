# PH-25.2 — The Battery On The Served Record, Across A Restart And A Resume

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-25.2
Parent: [PH-25](PH-25-production-record-battery.md)
Status: APPROVED
Approved: 2026-09-05 — from the evidence in §6
Created: 2026-09-05

---

## 1. Objective

Run the adversarial battery on a record that came over a socket from a spawned
`apps/api`, and prove the instrument sees through the wire: a served record
built to leak is called `exploitable`, and the venue's own served record is not.

## 2. What is built

**`servedAssurance(record, at, options)`** in `packages/lab` — the standing
verdict (`runStandingAssurance`) applied to a `ServedRecord`: the ticks are the
record's ticks, the seam indices come from the record's own discontinuities,
and nothing is read that the wire did not carry. The verdict names what it
could not build.

Three tests:

1. **Through the wire, a leak is still a leak.** The unit fake serves a
   Thue–Morse record — every sign the parity of its block index — and the
   verdict is `exploitable`. The same frames with fair signs are not.
2. **The venue's served record.** A spawned service, the fastest tape, a
   record the length a test can afford; the verdict is `clean` or `undecided`
   and never `exploitable`, every horizon reports its detection floor, and the
   floor is recorded rather than asserted — at three thousand ticks the
   battery cannot see a product-margin edge, and the verdict must say so.
3. **Across a restart.** The record read before a SIGKILL and the record read
   after the resume, joined as one dataset with the seam index where the
   observer recorded it; the withheld seam family is built and named in
   `families`, and the verdict is not `exploitable`.

## 3. Acceptance criteria

1. A planted leak served over the fake wire is `exploitable`; watched.
2. The real venue's served record is never `exploitable` at the sizes read.
3. A restart seam reaches the battery as a seam family, from the record's own
   discontinuities and nothing else.
4. Every verdict carries its detection floor per horizon, and the evidence
   file records the floors measured at the sizes read.

## 4. What was built

| Piece                                                                              | Where                                                      |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| `servedAssurance(record, {at, reference?, battery?})`                              | `packages/lab/src/served/servedAssurance.ts`               |
| `seamIndicesOf(record)` — from discontinuities and told gaps                       | same                                                       |
| `joinServedRecords(earlier, later)` — one record across a restart                  | same                                                       |
| The wire plants: a Thue–Morse record served as frames                              | `packages/lab/src/served/servedAssurance.test.ts`          |
| The socket half: the verdict on the venue's record, alone and joined across a kill | `apps/api/src/servedRecord.stat.test.ts` (PH-25.2 section) |

## 5. What the battery said through the wire

**A leak is still a leak.** Served as 36,000 frames over the fake wire, the
record whose direction is the Thue–Morse parity of its hour is `exploitable`
with every family available (the seam family built from the record's own
jump), worst |z| > 10. The fair walk served the same way is not, and its
verdict names the one family it could not build — `wh-seam-proximity`,
because an uninterrupted read has no seam — rather than running it on
nothing.

**The venue's record, at test size.** 1,800 ticks of the fastest tape read over
the socket (2.8 minutes of market time): `undecided`, 0 hypotheses tested,
detection floors 30s 80.87pp on 3 samples and 1m 140.08pp on 1 — the battery
saying, in its own numbers, that a record this short can decide nothing, which
is the verdict a test-sized read must produce. Nothing was `exploitable`.
Measured on the sixth run of the file, printed by the test.

**Across the kill.** The read before the SIGKILL and the read after the
resume, joined: 2,100 ticks, one seam at index 1,800 — read off the told gap
at the join, not supplied — 27 families built including `wh-seam-proximity`,
the only unavailable family `wh-cross-asset` (no reference record was given),
`undecided`, not `exploitable`.

## 6. Evidence

| Check                                                                                                                  | Result                                                                                    |
| ---------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `npx vitest run --project unit packages/lab/src/served` (20 tests)                                                     | 20 passed                                                                                 |
| Plants: seams not read from the record; a join that does not check the continuation; the joined gap left at index zero | each failed its test by name, restored                                                    |
| `npx vitest run --project statistical apps/api/src/servedRecord.stat.test.ts`                                          | 2 passed, 345.9 s (sixth run of the file; the PH-25.2 section ran on the fifth green run) |
| `npx tsc -b apps/api`, `npx eslint packages/lab/src/served apps/api/src/servedRecord.stat.test.ts`                     | clean                                                                                     |
