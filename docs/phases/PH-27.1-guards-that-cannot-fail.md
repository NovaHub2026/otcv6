# PH-27.1 — The Guards That Cannot Fail: Found By Planting, Fixed By Anchoring

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-27.1
Parent: [PH-27](PH-27-review-and-improvement.md)
Status: APPROVED
Approved: 2026-09-05 — from the evidence in §6
Created: 2026-09-05

---

## 1. Objective

Take every guard the project's own records name as unable to fail — a test
that passes by having nothing to say, a docstring claim no test holds, a
threshold written against a constant — and either watch it fail against a
planted defect or make it able to. Nothing is closed by reading: each item
below is re-executed on this tree before it is believed, because the list it
comes from (Cycle Audit 8's remaining-items map, `~/.otc-audit8/`) predates
the closing of that audit and several entries were resolved after it was
written.

## 2. Method

For each candidate: (1) find the guard or its absence in the tree; (2) plant
the defect the guard exists for; (3) run the test; (4) if it stays green,
anchor the guard — on a behaviour, never on a spelling or a constant — and
repeat until the plant fails it by name; (5) restore and re-run. A candidate
that already fails its plant is recorded as **holds** with the plant named,
and nothing else is done to it.

## 3. Candidates

From Cycle Audit 8's remaining-items map, each re-checked on this tree on
2026-09-05 before the subphase began. Most were closed inside the audit's own
closing and are recorded here as **holds** once their plant is named; the
rest are this subphase's work.

| Candidate                                                                         | First scan                                                               |
| --------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| §36's reachability bands unpinned                                                 | `reachabilityBand` pinned in 4 tests since a8 — verify by plant          |
| Nothing boots `LabModule`                                                         | one test boots it; `lab.module.ts` names `labProviders.test.ts` — verify |
| `LabPositions.actual` swallows settlement exceptions                              | `pending`/`evicted` distinguished and tested — verify by plant           |
| A torn last line in the Lab session file destroys the next process's first record | **no test mentions a torn line** — this subphase's                       |
| Minute-bucket statistics assume contiguous minutes                                | ten `minute` sites in `lab.controller.ts` — verify by plant              |
| `unitSteps ?? 1`                                                                  | removed (a4), a comment marks the site — holds                           |
| `SelectableArrival.seek`'s release-on-seek rule                                   | tested (a6) — verify by plant                                            |
| The engine timeline's transitions asserted only monotonically                     | 12 assertions now — verify by plant                                      |
| `MEASURED_BYTES_PER_TICK`, `MAX_MULTIPLEXED_ASSETS` unpinned                      | both in tests (a3) — verify by plant                                     |
| The state guard sorted by number, not chronology (found by PH-25.1)               | anchored in `535a6d0`, watched failing — holds                           |

## 4. Acceptance

1. Every row above ends as **holds** (plant named) or **anchored** (guard
   changed, plant named, watched failing then passing).
2. No guard is anchored on a spelling or a constant.
3. The unit suite and every touched statistical file green on the tree.

## 5. What the plants found

Every candidate was planted on this tree on 2026-09-05, the named test run,
and the tree restored (`git status` clean of anything but this subphase's own
edits after each). **Holds** means the plant failed the test by name.

| Candidate                                                                                     | Plant                                                                                               | Verdict                                                                                                     |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| §36's reachability bands                                                                      | `if (rate >= 0) return 'easy'`                                                                      | holds — 3 tests in `closeSelection.test.ts` name the band each rate reads                                   |
| `LabModule` boots                                                                             | `LabPositions` removed from `providers`                                                             | holds — "names a provider for every constructor parameter type"                                             |
| `LabPositions.status` swallows refusals                                                       | every `Error` returned as `pending`                                                                 | holds — 2 tests in `positions.test.ts`                                                                      |
| A torn last line in the session file                                                          | the seal removed (`const seal = ''`)                                                                | holds — "a crash that tore the last line costs that line and nothing after it" (the map predates this test) |
| `SelectableArrival.seek` releases                                                             | the `release()` call removed                                                                        | holds — "a seek releases"                                                                                   |
| The engine timeline's transitions                                                             | the regime-change branch deleted                                                                    | holds — 2 tests in `engineEvents.test.ts`                                                                   |
| `MEASURED_BYTES_PER_TICK`                                                                     | × 1.46                                                                                              | holds — "holds the window its docstring says, at the cost it says"                                          |
| `MAX_MULTIPLEXED_ASSETS`                                                                      | × 2                                                                                                 | holds — the refusal test in `adminSurface.test.ts`                                                          |
| **Minute-bucket statistics assume contiguous minutes**                                        | a record with a quiet minute in the middle (new tests, run first)                                   | **failed** on both sites — anchored, §6                                                                     |
| `unitSteps ?? 1`                                                                              | removed in a4; a comment marks the site                                                             | holds                                                                                                       |
| The state guard's chronology                                                                  | `CURRENT_STATE` naming PH-26.4                                                                      | holds since `535a6d0` (PH-25.1)                                                                             |
| This cycle's own guards: catalogue reproduction, personality fingerprint, stratified sampling | a compiled trait edited by hand; a fingerprint blind to the traits; a sample that is the first five | holds — 3, 1 and 3 tests respectively                                                                       |

**The one that could not fail, anchored.** `tickGranularity` (`packages/lab`)
and `medianCandleRange` (`apps/api/src/lab/distance.ts`) built minute buckets
from ticks alone, so a complete minute with no tick was not a minute: the
count was short, the zero never reached `ticksPerMinute`, the minute after a
hole had its opening gap measured against the close _before_ the hole as if
adjacent (a quiet market read as a gappy chart), and the distance unit's
median range was biased upward on every quiet market. Both now fill the span:
a quiet minute is a candle with no ticks and range zero, counted;
`GranularityReport.quietMinutes` names how many; boundary ratios are taken
only between adjacent traded minutes. Shown on the Lab's quality screen
("velas de 1m completas medidas · N sin ningún tick", `lab-granularity-minutes`)
and asserted in the browser suite.

## 6. Evidence

| Check                                                                              | Result                                                                       |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| The two new tests, run before the fix                                              | both failed (`expected 3 to be 4`; `{ range: 80, minutes: 2 }`)              |
| The same two after the fix, with every Lab unit test                               | `apps/api/src/lab` + `granularity.test.ts`: 136 passed                       |
| `npx tsc -b apps/api`, `npm run typecheck:web`, `npx eslint` on every touched file | clean                                                                        |
| `apps/web/src/lab.stat.test.ts` with the browser (the quality screen's new row)    | 9 flows passed, 186.5 s, with `OTC_REQUIRE_BROWSER=1` and the library prefix |
| Eleven plants above, each restored                                                 | as tabled                                                                    |
