# PH-29.4 — The Reference Client And Settlement Library: Exercised By The Release, #11 Decided

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-29.4
Parent: [PH-29](PH-29-integration-boundary.md)
Status: APPROVED
Approved: 2026-09-06 — from the evidence in §6
Created: 2026-09-06

---

## 1. Objective

Give a broker's engineer the client they would otherwise write: one class over
`fetch` that subscribes with an exact resume, reads history, asks the two
settlement prices and verifies a proof — and a settlement library whose money
is an integer in a named unit (Issue #11), so that the reference settlement a
broker embeds cannot round differently from theirs.

## 2. What is built

**`VenueClient`** in `@otc/client`: `health()`, `markets()`, `catalogue()`,
`history(id, timeframe, from, to)`, `tick(id, sequence)`, `priceAt(id, at)`,
`proof(id, sequence)` returning a verified proof or a named refusal, and
`subscribe(id, { from, onGap })` — an async iterator of ticks that reconnects
from the last delivered sequence plus one and surfaces a told gap as an event
rather than a hole. Every method checks the response against the contract's
shape and throws a `ContractViolation` naming the route and the departure.

**Money in `packages/trading` (Issue #11).** `Contract.stake` is an integer
in the broker's minor unit and `payoutRatio` a decimal of at most four places
— the fields keep their names, the decision log says why — and the settlement's
`returned` and `net` are integers computed exactly (`payoutMinor`: the payout
truncated to the minor unit); `tally` sums integers. Recorded in the decision
log before the code changed.

**The release exercises the client**: `conformance.stat.test.ts` reads the
venue through `VenueClient`, and the reference settlement of a contract opened
at a price the client asked for and expired at a price the client asked for
equals `settle()` over the ticks the client subscribed to.

## 3. Acceptance criteria

1. `VenueClient.subscribe` across a served gap yields the gap as an event and
   continues from `resumesAt`; across a dropped connection it resumes from the
   last delivered sequence plus one with no duplicate and no hole.
2. Settlement in minor units: a hundred thousand cent stakes agree with exact
   rational arithmetic; the floating-point disagreement the audit measured (124
   of 100 000) is zero.
3. The client refuses a response that departs from the contract, by name.
4. Every guard watched failing.

## 4. What was built

| Piece                                                                                           | Where                                                                        |
| ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `VenueClient`, `ContractViolation`, `Refusal`, `subscribe()` with exact resume                  | `packages/client/src/venueClient.ts`, `.test.ts`                             |
| `streamFrames`: the stream frame by frame, live; `readStream` on top of it                      | `packages/client/src/sse.ts`                                                 |
| `payoutMinor`, `PAYOUT_RATIO_SCALE`; integer stakes; exact settlement, exposure and limiter     | `packages/trading/src/contract.ts`, `settle.ts`, `exposure.ts`, `limiter.ts` |
| The Lab's positions take whole minor units                                                      | `apps/api/src/lab/positions.ts`                                              |
| The fake venue's dropped-connection and repeat-on-resume faults                                 | `packages/client/src/conformance.test.ts`                                    |
| The release exercises the client: settlement through it equals `settle()` over its subscription | `apps/api/src/conformance.stat.test.ts`                                      |

## 5. What the subphase found

1. **A live subscription cannot be built on a collecting read.** `readStream`
   returned at the end of a stream; a subscription must yield as frames
   arrive, so the reader became a generator and the collecting read a
   consumer of it.
2. **The dedup guard needs a venue that repeats.** The fake venue resumed
   exactly, so a client that yielded repeats passed the resume test; the fake
   gained a repeat-on-resume fault and the guard has a subject.
3. **The floating-point form returned a fraction of a minor unit for 95 000
   of 100 000 cent stakes** — not a rounding disagreement in 124 of them, as
   the audit had counted after rounding, but a number no ledger can hold.
4. **The contract's fields kept their names.** A `stakeMinor` and a rational
   payout would have reached twenty files including the Lab's screen; the
   meaning and the arithmetic changed, the shape did not.

## 6. Evidence

| Check                                                                                                    | Result                                                |
| -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| `packages/client` unit (the client, the conformance suite, the reader) with `packages/trading`           | 87 passed; then 15 client tests with the repeat fault |
| Plants: repeats yielded after a resume; the payout in floating point; a proof not verified               | failed by name (1 each), restored                     |
| Targeted gate: `format:check`, `build`, typechecks, `lint`, `npm run test:unit`, then `conformance.stat` | _see the approval commit_                             |
