# PH-34.2 — Integrated Verification, And What A Broker Must Know To Upgrade

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-34.2
Parent: [PH-34](PH-34-the-markets-tempo.md)
Status: APPROVED
Created: 2026-09-22
Approved: 2026-09-22 — `GATE_EXIT=0` on `3412049`, the phase tip: unit 168 files / 3,478 tests, coverage floors held, statistical 47 files / 411 tests with a real browser

---

## 1. Objective

Close PH-34: verify the phase as a whole, say in the broker's own guide what
changes under a running deployment, and release it.

## 2. What is done here

- **The integrated phase verification**: the full gate on the phase tip, with
  the battery and the mirror test inside it, and hosted CI on the merge — the
  two layers `GOVERNANCE.md` §34 asks for.
- **The integration guide** (`docs/integration/INTEGRATION.md`, the broker's,
  in Spanish): the tick rates an integrator should expect per asset and per
  regime, the refund rate that halved, and what the upgrade does to a running
  market — one seam per asset, no price jump, because every lattice is kept
  and the engine model is part of the checkpoint fingerprint.
- **The release**: `v2.2.0` tagged after hosted CI is green on the merge, with
  its record beside the others in `docs/evidence/`.

## 3. What this subphase may not do

Change the engine. Anything the verification finds is a finding about PH-34.1,
recorded and fixed there, not smoothed over here.

## 4. What the verification found

One failure in 411 statistical tests, and a real defect rather than a stale
number: **a hole the engine had named disappeared from the chart the moment
the market went quiet**. The stall notice — three mean intervals without a
tick — replaced the status outright, taking the hole's bounds with it. At
PH-34's tick rates a calm asset pauses that long in the ordinary course, so
what had been a rare race became the common case, and the browser suite caught
it here. The bounds are now carried by every status that follows them
(`PreviewChart.tsx`), and the test waits for the restarted venue to be serving
before it looks, because a chart that connects to a venue with nothing
published is a different case from the one it asserts.

Nothing else moved. The engine is untouched by this subphase.

## 5. Approved from evidence

`GATE_EXIT=0` on `3412049` with nothing edited under it: unit 168 files /
3,478 tests (40 s), coverage floors held (118 s), statistical 47 files / 411
tests (3,553 s) with a real Chromium. The phase's own claims are measured in
[`PH-34-THE-MARKETS-TEMPO.md`](../evidence/PH-34-THE-MARKETS-TEMPO.md), and
the broker's guide carries what changes on upgrade
(`docs/integration/INTEGRATION.md` §3.1).

Hosted CI on the merge is the corroborating layer (`ADR-0009`), and `v2.2.0`
is tagged only after it is green on both jobs — the rule `RELEASE-2.0.0.md`
was written to record.
