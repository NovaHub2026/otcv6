# PH-29.3 — The Conformance Suite: The Integration Checklist Made Executable Against A Live Venue

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-29.3
Parent: [PH-29](PH-29-integration-boundary.md)
Status: APPROVED
Approved: 2026-09-06 — from the evidence in §6
Created: 2026-09-06

---

## 1. Objective

Let a broker check its own deployment the way this repository checks a spawned
one: one command against a live venue, every contracted route exercised, the
stream's resume and gap semantics, the price rule against the stream, a proof
verified against the publisher's key, one report and an exit code.

## 2. What is built

**`@otc/client`** (`packages/client`) — the package a broker embeds, opened
here with what conformance needs and finished in PH-29.4: the contract moves
into it (`apps/api` imports it back, so the venue and its clients share one
definition), the shape checker with it, and `conformance(baseUrl)` — a pure
function over `fetch` that returns a report: per check, pass or fail with the
reason; overall, the venue's version against the client's.

**The checks.** `/health` answers and its `apiVersion` is the client's;
`/contract`'s digest is the client's; every contracted JSON route answers with
exactly the contracted keys and types (the same walk as `contract.test.ts`,
over HTTP); the stream delivers strictly increasing sequences, a resume from
`M+1` continues exactly, a resume from `0` with `onGap=live` is told a gap that
names `resumesAt` and is followed by the retained window; `price?at=` equals
the last tick at or before the instant over the ticks the stream delivered, for
a hundred instants; a proof for a committed sequence verifies with
`verifyInclusion` and `verifyCommitment` against `/health`'s publisher key —
or the venue says it does not publish, which is a pass with a note.

> **Corrected 2026-09-06 (Cycle Audit 10, a4-03 and a4-04).** Two claims in
> that paragraph were not what the suite did.
>
> **The key.** `/health` carries no publisher key — it never has; the
> contracted shape is `{status, assets, stalled, bootNonce, apiVersion,
ready}`. What the suite actually did was verify the signature against
> `publisherPublicKey` **from the same proof response**, which is exactly the
> forgery `signing.ts` warns about: a venue signing with a key of its own and
> naming that key passed with `ok: true`, and an auditor built one and got it.
> The key is now an input — `conformance({publisherPublicKey})` and
> `npm run conformance -- --key HEX`, told out of band from the operator's
> `publisher.json` — and when it is not given the check is still run but is
> named _"proof verifies against the key the venue names (not independent)"_
> and the report states above its table that no key was told. A PASS without
> `--key` says the proof is internally consistent, and nothing about who
> published the tick.
>
> **The routes.** "Every contracted JSON route answers with exactly the
> contracted keys and types" was the whole of the route walk: content was
> never compared with the stream that had just delivered it, so a venue
> answering `/ticks/1` with another tick's instant and price, or reporting a
> tick from far behind the live edge as its market's current one, passed. The
> suite now samples ticks the stream delivered and asks the record for them by
> sequence, and checks that `/markets/:id` is not behind the newest sequence it
> streamed.

**`npm run conformance -- --base URL [--key HEX] [--out report.md]`** in
`tools/sim`: runs it, writes the report, exit 0 when every check passes, 1
otherwise, 2 on a usage error.

## 3. Acceptance criteria

1. Against a spawned production venue with a record and publication on, every
   check passes and the report says so; the exit is 0.
2. Against a venue whose contract version differs, or whose `/markets/:id`
   carries an uncontracted key, the report names the check and the exit is 1.
3. The suite reaches nothing but the public surface (no engine, no runtime
   import; the lab's boundary scan applied to `packages/client`).
4. Every guard watched failing.

## 4. What was built

| Piece                                                                                                                        | Where                                                                             |
| ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `@otc/client`: the contract (moved), `conforms`/`shapeProblems`, `SseParser`/`readStream`, `conformance`/`renderConformance` | `packages/client/src/*`                                                           |
| The venue imports the contract back                                                                                          | `apps/api/src/contract.ts`                                                        |
| `npm run conformance -- --base URL [--out] [--ticks]`                                                                        | `tools/sim/src/conformanceTool.ts`, `.test.ts`, `package.json`                    |
| The package in the workspace: references, dependency policy, scan roots, coverage floor                                      | `tsconfig.json`, `dependencies.test.ts`, `guardrails.test.ts`, `vitest.config.ts` |
| A faithful fake venue and five unfaithful ones; the SSE reader against a local server                                        | `packages/client/src/conformance.test.ts`, `sse.test.ts`, `shape.test.ts`         |
| The suite against the shipped service, production-composed, with publication on                                              | `apps/api/src/conformance.stat.test.ts`                                           |

## 5. What the subphase found

1. **Three guards said where a new package must be declared** — the dependency
   policy, the replayable scan roots and the coverage floors — and the layout
   block in `CLAUDE.md`; each refused the package by name until it was.
2. **A client needs its own SSE parser.** The lab's is the same code and the
   lab cannot be shipped to a broker; the reader is written from the
   specification again and tested against a server that splits frames mid-line.
3. **Each check is proved to fail for its reason**: a fake venue with one fault
   at a time — another version, an extra key, a skipped sequence, the wrong
   price rule, a proof that does not verify — fails exactly the named check.

   > **Corrected 2026-09-06 (Cycle Audit 10, a4-04).** Not each: the proof
   > check's `agrees with the stream` clause had no fault of its own, and an
   > auditor removed the clause with the matrix still green. The fake venue
   > now also serves a valid proof of a tick nobody asked about, answers one
   > sequence with another tick's content, reports a market behind its own
   > stream, and signs with a key of its own while naming it (a4-03).

4. **The first run against the shipped service found a wrong refusal**: a
   market that publishes but has closed no window yet answered `404 does not
publish` to a proof request, where the contract says `409 not yet
committed`. The archive's reader now tells a registered asset's directory
   from an absent one; the suite passed the check either way because both are
   listed refusals — which is why the detail column is in the report.

## 6. Evidence

| Check                                                                                                                             | Result                                                                                                            |
| --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `packages/client` unit (shape, SSE, conformance) with the sim command and every guardrail                                         | 654 passed                                                                                                        |
| Plant: the version check dropped                                                                                                  | the "another contract version" test failed by name, restored                                                      |
| Coverage of `packages/client/src`                                                                                                 | 86.5% lines; floor set at 80                                                                                      |
| `conformance.stat.test.ts` against the spawned service (30 assets, publication on)                                                | PASS on the first run, every check; 60 ticks read, 101 instants agree, resume exact (§5.4 on the proof's refusal) |
| Targeted gate: `format:check`, `build`, `typecheck:web`, `typecheck:config`, `lint`, `npm run test:unit`, then `conformance.stat` | all green — 157 files / 3,187 tests; the spawned-venue suite passed again with the proof answering 409            |
