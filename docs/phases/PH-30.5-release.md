# PH-30.5 — Every Open Issue Closed Or Decided, The Package Regenerated, `v1.0.0` Tagged And Recorded

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-30.5
Parent: [PH-30](PH-30-release.md)
Status: APPROVED
Approved: 2026-09-06 — from the evidence in §6
Created: 2026-09-06

---

## 1. Objective

Leave nothing open by accident. Every Issue on the tracker is closed with a
fix, or closed with a decision recorded where the next reader will find it, or
left open **by name** with whose it is. The integration package is regenerated
from the tagged commit and validated standalone. The release is a record — the
commit, the gate, the served verdict, the conformance run, the restart the run
included, the ceilings a broker plans against — not a sentence in a handoff.

## 2. What is run

- The Issue tracker is walked once. For each open Issue: fixed in scope
  (#20, the Int32 narrowing), closed by decision (#4, #7, #8, #12, recorded in
  `DECISION-LOG.md` under 2026-09-06), or left open by name (#9 the multi-node
  composition, deferred by the Cycle 10 plan; #3 and #14, Governance
  amendments, the Human Owner's).
- `tools/sim/scripts/integration-package.sh v1.0.0 <out>` regenerates the
  package from the tag; the package's own unit suite is run standalone.
- `docs/evidence/RELEASE-1.0.0.md` records the release: commit, gate exit and
  duration, hosted CI, the served verdict (PH-30.4) and the conformance run,
  the restart the run included, the observer ceiling (PH-30.3), the record's
  cost per tick (PH-28), the contract version and digest, and what is
  deferred by name.
- The phase gate runs on the closing tree; the phase is approved from it; the
  merge to `main` is tagged `v1.0.0`.

## 3. Acceptance criteria

1. No open Issue without a recorded decision or a named owner.
2. The regenerated package builds and its unit suite passes standalone, with
   the file count and test count recorded.
3. `RELEASE-1.0.0.md` exists, indexed in `DOCS_INDEX.md`, and every number in
   it points at the record it comes from.
4. `v1.0.0` is a tag on the merge commit, and `CURRENT_STATE.md` names it.

## 4. What was run

1. The Issue tracker walked (`gh issue list --state open`): #16 closes at
   this merge (PH-30.2); #9, #3 and #14 stay open by name; everything else
   was closed earlier in the phase with a fix or a recorded decision
   (`DECISION-LOG.md`, 2026-09-06, "The release closes or decides every open
   Issue").
2. `docs/evidence/RELEASE-1.0.0.md` written from the records: the served
   verdicts and the conformance runs (PH-30.4), the observer ceiling
   (PH-30.3), the record across a kill (PH-28), the chains after the
   restart, the contract version and digest; the gate and the commit filled
   from the phase gate on the closing tree.
3. PH-30 §8 and §9 written; the phase gate run on the closing tree; the
   phase approved; the merge to `main` tagged `v1.0.0`.
4. `tools/sim/scripts/integration-package.sh v1.0.0 <out>` regenerated from
   the tag and its unit suite run standalone.

## 5. What the subphase found

1. **The phase gate refused the closing tree twice before it ran**: a
   subphase document Prettier had never seen (PH-30.3), and the guide's two
   examples, which live outside every tsconfig project and which the
   type-aware lint could not resolve. The first is formatted; the second is
   ignored by name with the reason in `eslint.config.js`. Both were the
   release's own additions, and the gate is what found them.
2. **A test named in a verification table did not exist** (a unit test for the
   observer fleet driver that was never written):
   the fleet driver is held by its recorded runs and has no unit test, and the
   table says so now rather than name a file the documentation guard would
   have refused at the approval commit.

## 6. Evidence

| Criterion                                                                  | Evidence                                                                                                                                                                                                                                              |
| -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. No open Issue without a decision or an owner                            | `gh issue list --state open` at the merge: #9 (deferred, Cycle 10 plan), #3 and #14 (Human Owner); #16 closed by the merge                                                                                                                            |
| 2. The regenerated package builds and its unit suite passes standalone     | `tools/sim/scripts/integration-package.sh` from the closing tree `6f1efa9`: 158 files, `npm ci` + `npm run build` + the unit suite standalone, **158 files / 2,816 tests passed**, exit 0; regenerated from `v1.0.0` after the tag as the deliverable |
| 3. `RELEASE-1.0.0.md` exists, indexed, every number pointing at its record | `docs/evidence/RELEASE-1.0.0.md`, `DOCS_INDEX.md`                                                                                                                                                                                                     |
| 4. `v1.0.0` tags the merge commit; `CURRENT_STATE.md` names it             | `v1.0.0` on the PH-30 merge commit on `main` (`git rev-parse v1.0.0`); `CURRENT_STATE.md` names it                                                                                                                                                    |
