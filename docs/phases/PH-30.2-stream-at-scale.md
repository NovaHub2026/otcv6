# PH-30.2 — The Stream At Scale: One Multiplexed Stream (#16); The Operator's Panel With Holes And A Retired Market

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-30.2
Parent: [PH-30](PH-30-release.md)
Status: APPROVED
Approved: 2026-09-06 — from the evidence in §6
Created: 2026-09-06

---

## 1. Objective

Close Issue #16 on the panel and carry a8-12: the panel opens **one**
multiplexed stream per page rather than one connection per chart, so eight
charts fit inside a browser's six connections; and the operator's chart shows
a told hole as a hole and a retired market as retired, with the browser suite
asserting both.

## 2. What is built

- **`apps/web/src/lib/marketStream.ts`** learns `/markets/stream?assets=…`:
  a `MultiplexedStream` that opens one `EventSource` for a set of assets,
  demultiplexes frames by `asset`, tracks per-asset delivered sequences,
  resumes the whole set from each asset's last sequence plus one, and
  surfaces a `gap` frame per asset with `resumesAt`. Charts subscribe to it
  by asset; the page owns the connection.
- **The chart** paints a told hole as a bounded hole (from the requested
  sequence to `resumesAt`) in the status and on the canvas, and a market
  whose `/markets/:id` answers `404` after being listed shows `retired`
  rather than `reconnecting`.
- **The browser suite** opens a page with eight charts and counts one
  connection; kills and restarts the engine under the page and asserts the
  hole's bounds appear; retires a market and asserts the status (a8-12).

## 3. Acceptance criteria

1. Eight charts on one page open one stream connection; each chart's ticks
   are contiguous and its own.
2. After a restart the page resumes every chart from its last sequence with
   no duplicate, and a told hole reads as a hole with its bounds.
3. A retired market's chart says so.
4. Every guard watched failing.

## 4. What was built

| Piece                                                                                     | Where                                                                     |
| ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `streamMarkets`: one connection, a window per asset, per-asset resume, hole, gap, retired | `apps/web/src/lib/marketStream.ts`, `.test.ts`                            |
| The board: eight cards on one stream; the _Tablero_ toggle                                | `apps/web/src/app/preview/Board.tsx`, `Preview.tsx`, `lib/es.ts`          |
| `otc_stream_connections` on `/metrics`                                                    | `apps/api/src/market.controller.ts`                                       |
| The proxy aborts the upstream stream with the browser's disconnect                        | `apps/web/src/app/engine/[...path]/route.ts`                              |
| The browser test: one connection for eight, a retired card, a bounded hole (a8-12)        | `apps/web/src/panel.stat.test.ts` (`restartEngineLosingRecord`, `metric`) |

## 5. What the subphase found

1. **Every chart the panel ever opened stayed subscribed on the engine.** The
   proxy route did not pass the browser's abort signal to its upstream fetch,
   so a closed `EventSource` left the engine writing to a dead proxy stream;
   the new metric read sixteen connections for a board of eight, the other
   eight being the charts of earlier screens. Fixed with one line and now
   observable on `/metrics`.
2. **A resume across a restart shows no hole any more** (PH-28), so the a8-12
   assertion needed the pre-PH-28 condition reproduced on purpose: the record
   deleted between the kill and the restart. Even then the board's cards, which
   resume from their last delivered sequence, see no hole — the resume point
   is above the checkpoint; the **single chart**, which resumes from the stored
   record's head, does, and that is where the bounded hole is asserted.
3. **A test appended by position landed twice** in the suite file and the file
   ran its tests twice, the second pass against a venue the first had retired
   an asset on; the duplicate is gone and the suite runs nine.

## 6. Evidence

| Check                                                                                                                     | Result                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `marketStream.test.ts` (the multiplexed client)                                                                           | 7 passed                                                                                                                                      |
| Plants: a retired market kept on the next connection; a hole told without bounds                                          | each failed by name, restored, 7 passed                                                                                                       |
| `panel.stat.test.ts` in a real browser                                                                                    | 9 passed — 1 connection, 8 subscriptions; the retired card; the chart read `hueco avisado por el motor: secuencias 483459–483475 no servidas` |
| The proxy leak, watched: the metric read 16 before the fix and 1 after                                                    | the suite's first run, then its last                                                                                                          |
| Targeted gate: `format:check`, `build`, typechecks, `lint`, `npm run test:unit`, then `conformance.stat` and `panel.stat` | _see the approval commit_                                                                                                                     |
