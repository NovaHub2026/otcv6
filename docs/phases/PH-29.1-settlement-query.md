# PH-29.1 — The Settlement Query: Price At A Sequence And At An Instant, With Its Inclusion Proof

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-29.1
Parent: [PH-29](PH-29-integration-boundary.md)
Status: APPROVED
Approved: 2026-09-06 — from the evidence in §6
Created: 2026-09-06

---

## 1. Objective

Let a broker ask the venue the two prices its settlement needs and prove them,
instead of keeping a copy of the stream and hoping it was complete.

## 2. What is built

Three production read routes, all from the tick record (PH-28.1) and the
publication directory, none from an engine:

- **`GET /markets/:id/ticks/:sequence`** — the tick, or `404` naming the
  record's bounds (`oldest`, `newest`) when the sequence is outside them. The
  live edge is the feed's; the past is the record's.
- **`GET /markets/:id/price?at=<instant>`** — the price in force at the
  instant: the **last tick at or before it**, the rule `settle()` and the
  charts use, with the tick's sequence and instant and the display price; a
  refusal when the instant precedes the record's oldest tick or is after the
  newest published (a price for a future instant is a prediction, not a
  record). The response names the rule.
- **`GET /markets/:id/proof/:sequence`** — the signed commitment of the
  window holding that sequence, the window's ticks (so a verifier can rebuild
  the root), the Merkle path (`proveInclusion`), and the publisher's public
  key; `409 not yet committed` while the window is open, `404` when the
  deployment does not publish. A verifier checks `verifyInclusion` and
  `verifyCommitment` with nothing but this response and the key it was told
  out of band.

The journal for a window is read from the publication directory
(`<from>-<to>.journal`), the commitment from `commitments.ndjson` by streaming
(`readCommitmentsStream`) — a proof costs a linear read of the file until
Issue #19's rotation exists, and the response says how long it took.

## 3. Acceptance criteria

1. `ticks/:sequence` returns the tick the stream served for that sequence,
   byte-identical fields; outside the record it refuses naming the bounds.
2. `price?at=` equals `settle()`'s `priceAtOrBefore` over the served record
   for a thousand instants, including instants between ticks and exactly on a
   tick; before the oldest tick and after the newest it refuses.
3. A proof verifies with the distribution package's verifiers against the
   publisher's key; a tampered tick in the response does not.
4. The three routes carry no cursor and no snapshot key (`productionResponses`).
5. Every guard watched failing.

## 4. What was built

| Piece                                                                                  | Where                                                              |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `TickRecord.atOrBefore` on both implementations; an instant index on the SQLite file   | `packages/runtime/src/tickRecord.ts`, `.test.ts`                   |
| `readJournalFile`, `proveFromPublication` (`proved` / `uncommitted` / `not-published`) | `packages/distribution/src/journalFile.ts`, `.test.ts`             |
| `PublicationService.proofFor`; the venue's `recordedTick`, `priceAt`, `recordBounds`   | `apps/api/src/publication.service.ts`, `venue.service.ts`          |
| The three routes and their refusals                                                    | `apps/api/src/market.controller.ts`                                |
| The rule held to a thousand instants and to `settle()`; proofs verified; refusals      | `apps/api/src/settlementQuery.test.ts`                             |
| The production-responses guard given a record and an archive, so the routes answer     | `apps/api/src/productionResponses.test.ts` (9 routes by value now) |

## 5. What the subphase found

1. **The production-responses guard was not seeing routes that refuse.** Its
   venue kept no record and published nothing, so the three new routes threw
   and were skipped — "not JSON or refused" — and a leak in them would have
   passed. It keeps a record and publishes into a scratch directory now.
2. **The journal reader is a second implementation of the lab's**, on purpose:
   `@otc/api` may not depend on the lab. It refuses the same shapes, and the
   test feeds it the writer's own files.
3. **A proof is refused when the archive and the record disagree** — an edited
   journal line does not get a proof, and the test edits one.

   > **Corrected 2026-09-06 (Cycle Audit 10, a4-05).** That held for the
   > **edited line only**, and only while the tick was still inside the
   > record's retention: the route compared the _requested_ tick with the
   > record and nothing else, so an edit on line 3 of a window left every
   > other sequence in it answering `200` with a signature that verifies and
   > an inclusion proof that does not, and the edited tick itself answered
   > `200` again once it had aged out of the record. `proveFromPublication`
   > now recomputes the window's Merkle root and refuses the whole window
   > (`409`) when it no longer hashes to the root its commitment signs, before
   > any comparison with the record. The record cross-check stays, for the
   > archive that is internally consistent and still contradicts what was
   > published — an operator who re-signed what they edited — and
   > `settlementQuery.test.ts` now builds exactly that.

> **Added 2026-09-06 (Cycle Audit 10, a7-03).** What this subphase did not
> record is what the index cost. `tick_by_instant` over
> `(asset_id, instant, sequence)` on a `WITHOUT ROWID` table repeats the primary
> key in every entry, and it took the record from **32.6 to 61.2 bytes a tick on
> disk** — measured on 2026-09-06 by building the same table over the same
> 20,000 ticks with and without it. The default of 250,000 ticks an asset is
> therefore 15.2 MiB an asset and 456 MiB for the catalogue of thirty, not the
> eight megabytes and quarter of a gigabyte PH-28 and PH-28.1 state; both now
> carry the correction. The cost was invisible for two phases because
> `tickRecord.test.ts` asserted a bound of 80 and no guard related its printed
> figure to the prose that sized `DEFAULT_RECORD_TICKS`. It does now
> (`MEASURED_RECORD_BYTES_PER_TICK`, held to within 20%).

## 6. Evidence

| Check                                                                                         | Result                                              |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `tickRecord.test.ts` (both implementations), `journalFile.test.ts`, `settlementQuery.test.ts` | 22 passed; with `productionResponses.test.ts` 27    |
| Plants: the price route one instant late; the proof route not comparing; the rule strict `<`  | failed by name (1, 1, 3 tests), restored, 22 passed |
| Targeted gate: `format:check`, `build`, `typecheck:web`, `lint`, `npm run test:unit`          | all green — 152 files / 3,156 tests                 |
