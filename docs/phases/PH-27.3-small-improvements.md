# PH-27.3 — The Small Improvements With No Owner, Closed

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-27.3
Parent: [PH-27](PH-27-review-and-improvement.md)
Status: PLANNED
Created: 2026-09-05

---

## 1. Objective

Close the improvements the records name that are a subphase or less and touch
no price path. Each is either done with a guard, or measured and sent to the
report as a phase.

## 2. Candidates

| Improvement                                                                                      | Source            |
| ------------------------------------------------------------------------------------------------ | ----------------- |
| `assessRealism` blocks the event loop for seconds on a million-tick run; a yield between metrics | PH-24 §9          |
| The browser clients do not read `resumesAt` from a gap frame and cannot bound the hole they show | PH-25.1 finding b |
| The panel opens one connection per asset while the multiplexed endpoint exists (Issue #16)       | PH-22 §8          |
| `aggregational-gaussianity` aggregates in ticks, not in time                                     | PH-24 §9          |
| Three spawned-service test files carry the same boot helper                                      | PH-25.1           |

## 3. Acceptance

1. Each candidate is closed with a guard watched failing, or measured and
   named in the report with what it would take.
2. The price path is untouched (the mirror test and `stepIndependence` green).
