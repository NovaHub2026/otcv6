# PH-28.1 — The Record Composed: Ticks Persisted, Replay Served From The Store Across A Restart

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-28.1
Parent: [PH-28](PH-28-durable-venue.md)
Status: APPROVED
Approved: 2026-09-05 — from the evidence in §6
Created: 2026-09-05

---

## 1. Objective

Make the served record outlive the process: a client that held sequence _n_
before a restart resumes from _n_ after it, and the candle the kill fell in is
stored whole. PH-25.1's findings a and c, closed by construction rather than
by a longer window.

## 2. What is built

**`TickRecord`** in `packages/runtime` — an interface with two
implementations, `SqliteTickRecord` (one database in the state directory,
WAL, schema-versioned like the candle history) and `MemoryTickRecord` (tests
and a runtime that must not persist):

- `append(batches)` — one transaction for every asset's batch of a scheduler
  pass. Per asset: sequences strictly increasing; a sequence at or below the
  head is **compared** with the stored tick and refused with `RecordForkError`
  if it differs; the fresh ticks are returned per asset. A gap above the head
  is accepted and is what a seam looks like in the record.
- `tail(assetId, limit)` — the newest contiguous run of at most `limit`
  ticks, for priming the feed.
- `since(assetId, fromSequence, limit)` — ticks at or after a sequence, for
  priming the recorder and for PH-29's settlement query.
- `head(assetId)`, `oldest(assetId)`, `trim(assetId, keep)`, `close()`.

**`VenueService`** takes a `TickRecord | null` (null: a deployment that keeps
none, like the history). In `tick()` the record is appended before the feed,
the publisher and the history observe the batch; only the ticks the record
reports fresh go onward. A fork stalls the market by name. On `start()` and
`host()` each market is primed: the feed with the record's tail, the recorder
with the record's ticks after the newest stored minute. `trim` runs on the
checkpoint cadence to the configured bound.

**`AppModule`** composes `SqliteTickRecord` at `OTC_RECORD_DB` (default
`<state dir>/record.db`), bounded by `OTC_RECORD_TICKS` per asset (default
`DEFAULT_RECORD_TICKS`), read the way the other environment values are.

## 3. Acceptance criteria

1. A spawned service, checkpointed, killed with `SIGKILL` and resumed, honours
   a resume from a sequence published before the checkpoint: the ticks come
   from the record, byte-identical to what the first process served.
2. After that kill the minute bar the kill fell in is stored, whole, with a
   `firstSequence` contiguous with its neighbours; the hourly rollup does not
   withhold the hour.
3. A tick republished after a resume is verified against the record and not
   served twice: the feed's window and the publisher's chain stay contiguous.
4. A planted disagreement — a recorded tick whose price differs from the one
   the market republishes — is refused, the market stalls with the fork
   named, and nothing is published for it.
5. The record holds sequence, instant and price and nothing else; the
   architecture scan finds no engine import in it, and no engine state in the
   database.
6. Every guard is watched failing against a plant.

## 4. What was built

| Piece                                                                          | Where                                                                   |
| ------------------------------------------------------------------------------ | ----------------------------------------------------------------------- |
| `TickRecord`, `SqliteTickRecord`, `MemoryTickRecord`, `DEFAULT_RECORD_TICKS`   | `packages/runtime/src/tickRecord.ts`                                    |
| Conformance over both implementations; the file's schema, version and row cost | `packages/runtime/src/tickRecord.test.ts`                               |
| The record appended before the feed; the fresh ticks published; a fork stalls  | `apps/api/src/venue.service.ts` (`tick`, `#recordPass`)                 |
| The feed and the recorder primed at `start` and `host`; trim on the checkpoint | same (`#primeFromRecord`, `checkpoint`), `history.service.ts` (`prime`) |
| `OTC_RECORD_DB`, `OTC_RECORD_TICKS` (at least the feed's window)               | `apps/api/src/app.module.ts`                                            |
| In-process: resume across a kill, the whole minute, the fork, no record        | `apps/api/src/venueRecord.test.ts`                                      |
| Over the socket: the kill scenario asserts the fixed behaviour                 | `apps/api/src/servedRecord.stat.test.ts`                                |

## 5. What the subphase found

1. **The row costs 32.6 bytes on disk**, measured and printed by the
   conformance test, so the default bound of 250 000 ticks is about eight
   megabytes per asset and a quarter of a gigabyte for the thirty.

> **Corrected 2026-09-06 (Cycle Audit 10, a7-03).** 32.6 bytes was right when it
> was measured and stopped being right at PH-29.1, which added the
> `tick_by_instant` index over `(asset_id, instant, sequence)`. Re-measured the
> same way on 2026-09-06 — one `SqliteTickRecord` on a real file, 20,000
> consecutive ticks of one asset, `stat().size / 20000` — the row costs **61.2
> bytes**, and the same table built by hand with and without that index costs
> 32.6 and 61.2, so the index is the whole of the doubling. At the shipped
> default of 250,000 ticks one asset's file is 15,941,632 bytes (63.8 bytes a
> tick, the per-row cost rising with the b-tree's depth): **15.2 MiB per asset
> and 456 MiB for the thirty**, not eight megabytes and a quarter of a
> gigabyte. The figure is now the exported constant
> `MEASURED_RECORD_BYTES_PER_TICK` in `packages/runtime/src/tickRecord.ts`, and
> `tickRecord.test.ts` holds the measurement to within 20% of it on every run —
> it previously asserted only a bound of 80, which 61.2 passed as happily as
> 32.6 did.

2. **A resumed market's republication is the ordinary case, not an edge.**
   Every restart after a checkpoint replays ticks the previous process
   served; before this subphase they reached the feed as new ticks of an
   empty window. The record's compare-and-drop is what makes the feed, the
   publisher's commitment chain and the recorder contiguous across it.
3. **The stat suite's kill scenario passed on its first run** with the
   assertions inverted: the read from `last + 1` was honoured with no gap
   told, and the stored minute bars around the kill are contiguous (333.5 s).
4. **`module.test.ts` refuses a constructor primitive with an explicit type**
   (`recordTicks: number`) as an unresolvable provider; the annotation was
   dropped, as the other defaulted primitives have none.
5. **The stale-build guard (CA9 a8-05) was red on a clean tree.** It compared
   the source's mtime with `dist`'s, and the merge and branch checkouts had
   rewritten an unchanged `servedAssuranceJob.ts` — the incremental build,
   seeing the same content, rightly emitted nothing, and the first targeted
   gate after Cycle Audit 9's merge failed one test of 3,063 for it. The guard
   now asks `tsc -b --dry` whether the sim project is up to date, which is
   tsc's own bookkeeping; watched failing against an unbuilt content edit.

## 6. Evidence

| Check                                                                                          | Result                                                                                                                                    |
| ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `npx vitest run --project unit packages/runtime/src/tickRecord.test.ts`                        | 17 passed (both implementations; 32.6 bytes/tick — 61.2 since PH-29.1, see §5)                                                            |
| `npx vitest run --project unit apps/api/src/venueRecord.test.ts`                               | 4 passed                                                                                                                                  |
| Plants: feed unprimed; recorder unprimed; the record not comparing; generated ticks served     | each failed by name (2, 1, 2, 2 tests), restored, 21 passed                                                                               |
| `npx vitest run --project unit apps/api packages/runtime packages/core/src/guardrails`         | 1,361 passed after the annotation fix (59 files)                                                                                          |
| `npx vitest run --project statistical apps/api/src/servedRecord.stat.test.ts`                  | 2 passed, 333.5 s — resume honoured across the kill, bars contiguous                                                                      |
| `npx tsc -b apps/api`, `npx eslint` on every touched file                                      | clean                                                                                                                                     |
| Targeted gate: `format:check`, `build`, `typecheck:web`, `typecheck:config`, `lint`, full unit | all green; the unit leg 3,062 of 3,063 on the first run (§5.5), then `npm run test:unit` 147 files / 3,063 tests green on the fixed guard |
