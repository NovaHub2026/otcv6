# PH-27.2 — The Records That Are Stale: Every "Leaves Open" Checked Against The Tree

Type: SUBPHASE TECHNICAL DOCUMENT
Identifier: PH-27.2
Parent: [PH-27](PH-27-review-and-improvement.md)
Status: APPROVED
Approved: 2026-09-05 — from the evidence in §5
Created: 2026-09-05

---

## 1. Objective

Every "what the phase leaves open" section from PH-20 onward, every open
GitHub Issue, and the architecture documents' own admissions, read against
the tree as it is — and each item marked **still open**, **closed by** (the
closing phase or commit named, with the guard that holds it), or **carried**
to PH-27.5's report. A record that says "open" about something a later phase
closed sends the next reader to rebuild what exists.

## 2. Sources

- `docs/phases/PH-20` §5, `PH-21` §8, `PH-22` §8, `PH-23` §10, `PH-24` §9,
  `PH-25` §7.
- GitHub Issues #2–#22 (open), each re-checked against the tree; those the
  tree closes are closed with the commit named; the two that are the Human
  Owner's (#3, #14) are left as they are.
- `docs/architecture/*.md` admissions ("not yet", "unmeasured").
- `CLAUDE.md` §5's numbers, re-measured from the PH-25 gate.

## 3. Acceptance

1. Each open-items section carries a dated line per item with one of the
   three verdicts and, for **closed by**, the guard that holds it.
2. No Issue remains open that the tree closes; none is closed by narrative.
3. The architecture documents mention the Lab and the served-record
   instrument where their headers say absence means nonexistence.

## 4. What was found

**Six sections, twenty items.** Each "leaves open" section from PH-20 to
PH-25 now ends in a dated block naming, per item, _closed by_ (with the guard
that holds it), _still open_ (with where the report carries it), or
_superseded_ (with what replaced it). Closed by the tree since they were
written: scale (PH-21, PH-26); B-030 (Issue #5's own rule); INV-007 at a
hundred (thirty, measured behaviourally); the CYCLE-7 catalogue and the
400-brief probe (superseded by PH-26's recorded draws); fan-out to two
thousand (PH-22); the Lab's interventions (PH-24). Still open and carried:
a hundred hosted, the panel's virtualisation and multiplexing, the replay
ceiling's engagement, ten thousand observers, socket memory, §67 jobs, the
replay screen wired to nothing, `aggregational-gaussianity` in ticks, a bias
across a restart, the multi-node composition.

**Issues.** Six closed on evidence in the tree, each with the commit or test
named in its closing comment: #2 (coverage measured by every gate since
PH-26), #5 (no recurrence in three cycles), #6 (the gate's three legs each
carry their exit code and the completion line names them), #17 and #18
(superseded by `PH-26-CATALOGUE-OF-THIRTY.md`), #21 (`multiAsset.stat.test.ts`
at thirty). Ten kept, each referenced from the report: #4, #7, #8, #9, #10,
#11, #12, #16, #19, #20. Two are the Human Owner's and untouched: #3, #14.

**Architecture and CLAUDE.md.** `OVERVIEW.md` already describes the Lab and
its boundary (§ "The OTC Market Lab") and the multiplexed stream — the map's
entry was stale about the map. _Acceptance 3 was recorded as met here and was
not (Cycle Audit 9, a7-04): no architecture document mentioned the
served-record instrument, and `CATALOGUE_AND_PANEL.md` still described
`onGap=live` as the live edge. Both are written now — `VALIDATION.md` § "The
served record", `CATALOGUE_AND_PANEL.md` §1.1 and §5.2 — and this sentence
records that the acceptance was met by the audit's fix, not by this subphase._ `CLAUDE.md` §5's numbers are re-measured from
the PH-25 gate (72.5 min statistical, 140 unit files / 2,973 tests in 110 s)
with the date on them.

## 5. Evidence

| Check                                                                         | Result                                      |
| ----------------------------------------------------------------------------- | ------------------------------------------- |
| `documentation.test.ts`, `stateConsistency.test.ts` after the six annotations | green (the run below)                       |
| Each closed Issue's comment names a test or an evidence file in the tree      | #2, #5, #6, #17, #18, #21                   |
| `gh issue list --state open`                                                  | 12 open: #3, #4, #7–#12, #14, #16, #19, #20 |
